import type { ProgressStylesNames } from '@pattern-ui/progress';

export const Progress: Record<ProgressStylesNames, string> = {
  root: 'Root element',
  bar: 'Filled bar',
  label: 'Progress label',
};
