---
group: 'pattern-hooks'
package: '@pattern/hooks'
title: 'use-isomorphic-effect'
category: 'lifecycle'
order: 1
slug: /hooks/use-isomorphic-effect/
description: 'Switch between useEffect during SSR and useLayoutEffect after hydration'
import: "import { useIsomorphicEffect } from '@pattern/hooks';"
docs: 'hooks/use-isomorphic-effect.mdx'
source: 'pattern-hooks/src/use-isomorphic-effect/use-isomorphic-effect.ts'
---

## Usage

use-isomorphic-effect allows you to switch between useEffect during server side rendering and useLayoutEffect after hydration.
Use it wherever you would use useLayoutEffect to avoid warnings during ssr:

```tsx
import { useIsomorphicEffect } from '@pattern/hooks';

function Demo() {
  useIsomorphicEffect(() => {
    document.title = 'title';
  });

  return null;
}
```
