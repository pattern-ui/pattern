---
group: 'pattern-core'
package: '@pattern-ui/core'
title: TypographyStylesProvider
order: 1
slug: /core/typography-styles-provider/
category: 'typography'
description: 'Add Pattern typography styles to your html content'
props: ['TypographyStylesProvider']
import: "import { TypographyStylesProvider } from '@pattern-ui/core';"
source: 'pattern-core/src/components/TypographyStylesProvider/TypographyStylesProvider.tsx'
docs: 'core/TypographyStylesProvider.mdx'
styles: ['TypographyStylesProvider', 'single-node']
---

import { TypographyStylesProviderDemos } from '@pattern-ui/demos';

## Usage

Pattern does not expose typography global styles.
Use TypographyStylesProvider to add Pattern typography styles to your html content:

```tsx
import { TypographyStylesProvider } from '@pattern-ui/core';

function Demo() {
  return (
    <TypographyStylesProvider>
      <div dangerouslySetInnerHTML={{ __html: '<p>Your html here</p>' }} />
    </TypographyStylesProvider>
  );
}
```

## Example

<Demo data={TypographyStylesProviderDemos.usage} />

## All styles demo

TypographyStylesProvider includes styles for:

- paragraphs
- headings
- lists
- blockquotes
- tables
- links
- images
- hr
- code and pre

<Demo data={TypographyStylesProviderDemos.all} />
