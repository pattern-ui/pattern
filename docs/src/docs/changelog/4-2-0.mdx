---
group: 'changelog'
title: 'Version 4.2.0'
order: 1
slug: /changelog/4-2-0/
release: https://github.com/pattern-ui/pattern/releases/tag/4.2.0
date: 'April 27th, 2022'
---

import {
  ThemeDemos,
  IndicatorDemos,
  CalendarDemos,
  ModalDemos,
  HooksDemos,
  SegmentedControlDemos,
} from '@pattern/demos';

## primaryShade on theme

New `theme.primaryShade` property is used to determine which shade will be used for the components that have `color` prop,
it defaults to 6. Note that for some variants with dark color scheme `primaryShade` will not be used as it will cause contrast issues.

<Demo data={ThemeDemos.primaryShade} />

`primaryShade` can also be customized for dark and light color scheme separately:

```tsx
import { PatternProvider } from '@pattern/core';

function Demo() {
  return (
    <PatternProvider theme={{ primaryShade: { light: 6, dark: 8 } }}>
      <App />
    </PatternProvider>
  );
}
```

## Indicator component

New [Indicator](https://pattern-ui.design/core/indicator/) component:

<Demo data={IndicatorDemos.configurator} />

## use-focus-within hook

New [use-focus-within](https://pattern-ui.design/hooks/use-focus-within/) hook detects if any element within has focus,
it works the same way as `:focus-within` CSS selector:

<Demo data={HooksDemos.useFocusWithinDemo} />

## use-network hook

New [use-network](https://pattern-ui.design/hooks/use-network/) hook returns current connection status:

<Demo data={HooksDemos.useNetworkDemo} demoProps={{ toggle: true }} />

## New features

[Calendar](https://pattern-ui.design/dates/calendar/) component now supports multiple selected dates:

<Demo data={CalendarDemos.multiple} />

[Modal](https://pattern-ui.design/core/modal/) and [Drawer](https://pattern-ui.design/core/drawer/) components now support setting overlay blur:

<Demo data={ModalDemos.overlay} />

[Calendar](https://pattern-ui.design/dates/calendar/), [Month](https://pattern-ui.design/dates/month/) and [DatePicker](https://pattern-ui.design/dates/date-picker/) components
now support custom day renderer with `renderDay` function:

<Demo data={CalendarDemos.renderDay} />

[SegmentedControl](https://pattern-ui.design/core/segmented-control/) component now supports disabled items:

<Demo data={SegmentedControlDemos.disabled} />

## Other changes

- [use-window-event](https://pattern-ui.design/hooks/use-window-event/) hook now supports custom events
- [use-local-storage](https://pattern-ui.design/hooks/use-local-storage/) hook now supports value sync in the same tab
- [RichTextEditor](https://pattern-ui.design/others/rte/) now supports placeholder
