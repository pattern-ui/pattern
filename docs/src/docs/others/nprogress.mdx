---
group: 'Other packages'
package: '@pattern-ui/nprogress'
title: 'Navigation progress'
order: 1
slug: /others/nprogress/
description: 'A navigation progress bar'
props: ['NProgress']
import: "import { NProgress } from '@pattern-ui/nprogress';"
source: 'pattern-nprogress/src'
docs: 'others/nprogress.mdx'
installation: '@pattern-ui/nprogress'
license: MIT
---

import { NProgressDemos } from '@pattern-ui/demos';

## Installation

Package depends on @pattern-ui/core and @pattern-ui/hooks.

Install with npm:

```sh
npm install @pattern-ui/nprogress @pattern-ui/core @pattern-ui/hooks
```

Install with yarn:

```sh
yarn add @pattern-ui/nprogress @pattern-ui/core @pattern-ui/hooks
```

## Setup NProgress

Add `NProgress` to your app:

```tsx
import { PatternProvider } from '@pattern-ui/core';
import { NProgress } from '@pattern-ui/nprogress';

function App() {
  return (
    <PatternProvider>
      <NProgress />
      <YourApp />
    </PatternProvider>
  );
}
```

## Usage

<Demo data={NProgressDemos.usage} />

## Event based functions

`@pattern-ui/nprogress` exports several functions which can be used to perform certain actions
from any part of your application:

```tsx
// All functions can be called from anywhere (not only from components)
import {
  addNProgress,
  decreaseNProgress,
  setNProgress,
  startNProgress,
  stopNProgress,
  resetNProgress,
} from '@pattern-ui/nprogress';

// Adds progress to the current progress
addNProgress(10);

// Decreases the current progress
decreaseNProgress(10);

// Sets a custom progress
setNProgress(50);

// Starts the progress bar
startNProgress();

// Stops the progress bar
stopNProgress();

// Resets the progress bar
resetNProgress();
```
